name: first git hub action
on : 
  push:
  
jobs:
  builds:
    runs-on: ubuntu-latest
    environment: Dev
    steps:
      - name: first step
        run: echo "Hello hEMANT"
      - name: 2nd step
        run: echo $DEV
    
